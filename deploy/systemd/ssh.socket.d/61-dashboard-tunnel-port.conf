[Socket]
# Ubuntu socket activation owns the listening sockets, so sshd_config alone is
# insufficient. Clear the vendor list before declaring both recovery and
# dashboard-tunnel listeners.
ListenStream=
ListenStream=0.0.0.0:22
ListenStream=[::]:22
ListenStream=0.0.0.0:2222
ListenStream=[::]:2222
