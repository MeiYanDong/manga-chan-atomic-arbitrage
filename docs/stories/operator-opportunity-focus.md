# Story: 经营面板只展示最值得看的机会

## 用户结果

作为经营者，我进入策略页时先看到当前最值得判断的少量路线，而不是被完整候选库淹没；后台扫描范围和公开 API 数据不因此缩小。

## 验收标准

- 面板先按执行阶段、报价新鲜度和初筛收益排序；
- 主表最多展示前 12 条路线；
- 候选超过 12 条时，明确显示仍在后台扫描的隐藏条数；
- `/api/v1/opportunities` 保持完整，不为适配页面而截断数据；
- 每条可见路线仍可打开原因说明；
- 空列表继续显示“暂无候选路线”，不得把未知状态写成零机会。
